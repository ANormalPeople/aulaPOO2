class ClientThread(threading,Thread):
    def __init__(self,clientAdress)
    
    
def run (self):
    self.nome = self.csockt.recive(1024).decode()
    print(self.name," se conectou! ")
    msg = ''
    sair = True
    while sair:
        data = self.csocket.recv(1024)
        msg = data.decode()
        print("from",self.name+": ", msg)
        if ((msg= 'bye') or (msg = None) or (msg == '')):
            self.csocket.send(msg.encode())
            sair = False
        print("cliente at ", clienteAddress, "disconected. . . ")
        
        if name == "__main__":
            localhosta  = "0.0.0.0"
            PORT = 7018
            
            while True:
                server.listen(20)
                
            
    